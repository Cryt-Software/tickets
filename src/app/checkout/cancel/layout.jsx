export const metadata = {
  title: 'Payment Cancelled | EventHub',
  description: 'Your payment was cancelled. You can try again or browse other events.',
};

export default function CheckoutCancelLayout({ children }) {
  return children;
}
